trigger: none

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Kubernetes@1
  displayName: "Test Kubernetes Connection"
  inputs:
    connectionType: 'Kubernetes Service Connection'
    kubernetesServiceEndpoint: 'aws-vm-kubernetes-service-connection'
    command: 'get'
    arguments: 'nodes'